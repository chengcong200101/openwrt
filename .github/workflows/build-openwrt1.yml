name: Build-OpenWrt-24.10.4-Custom

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    #==============================================================
    # 1. 初始化构建环境
    #==============================================================
    - name: Init build env
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo apt-get update
        sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib \
            gettext git libncurses5-dev libssl-dev python3 python3-pip \
            unzip zlib1g-dev file wget ccache jq rsync \
            subversion mercurial time libelf-dev cmake \
            autoconf automake libtool pkg-config texinfo gperf

    #==============================================================
    # 2. 设置 ccache 加速
    #==============================================================
    - name: Setup ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ${{ runner.os }}-ccache-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-ccache-

    - name: Enable ccache
      run: |
        echo "CCACHE_DIR=~/.ccache" >> $GITHUB_ENV
        ccache -M 2G
        ccache -s

    #==============================================================
    # 3. 克隆 OpenWrt 源码
    #==============================================================
    - name: Clone OpenWrt source
      run: |
        git clone --depth=1 https://github.com/openwrt/openwrt.git -b v24.10.4 openwrt

    #==============================================================
    # 4. 添加必要的第三方插件源
    #==============================================================
    - name: Add third-party feeds
      run: |
        cd openwrt
        
        # 添加必要的第三方源
        cat >> feeds.conf.default << "EOF"
        src-git openclash https://github.com/vernesong/OpenClash;master
        src-git passwall https://github.com/xiaorouji/openwrt-passwall;main
        src-git passwall_packages https://github.com/xiaorouji/openwrt-passwall-packages;main
        src-git smartdns https://github.com/pymumu/smartdns;master
        src-git argon https://github.com/jerrykuku/luci-theme-argon;master
        src-git ddnsgo https://github.com/sirpdboy/luci-app-ddns-go;main
        src-git lucky https://github.com/gdy666/luci-app-lucky;main
        EOF

    #==============================================================
    # 5. 预下载工具链
    #==============================================================
    - name: Pre-download toolchain
      run: |
        cd openwrt
        ./scripts/feeds update base
        ./scripts/feeds install base
        make tools/tar/compile -j1 V=sc || echo "Pre-download completed with warnings"

    #==============================================================
    # 6. 更新完整 feeds
    #==============================================================
    - name: Update Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    #==============================================================
    # 7. 导入您指定的插件配置（简化版本）
    #==============================================================
    - name: Load custom plugins config
      run: |
        cd openwrt
        cat > .config << "EOF"
        CONFIG_TARGET_x86=y
        CONFIG_TARGET_x86_64=y
        CONFIG_TARGET_x86_64_DEVICE_generic=y

        #================================
        # 基本系统
        #================================
        CONFIG_PACKAGE_luci=y
        CONFIG_PACKAGE_luci-base=y
        CONFIG_PACKAGE_luci-compat=y
        CONFIG_LUCI_LANG_zh_Hans=y

        #================================
        # 您指定的插件 - 分批次添加
        #================================
        # 第一阶段：基本系统插件
        CONFIG_PACKAGE_luci-app-attendedsysupgrade=y
        CONFIG_PACKAGE_luci-app-ttyd=y
        CONFIG_PACKAGE_luci-app-wol=y
        CONFIG_PACKAGE_luci-app-firewall=y
        CONFIG_PACKAGE_luci-app-upnp=y
        
        # 网络服务
        CONFIG_PACKAGE_luci-app-vsftpd=y
        CONFIG_PACKAGE_luci-app-zerotier=y
        
        # 主题
        CONFIG_PACKAGE_luci-theme-argon=y

        #================================
        # 必要的内核模块
        #================================
        CONFIG_PACKAGE_kmod-usb-storage=y
        CONFIG_PACKAGE_kmod-fs-ext4=y
        CONFIG_PACKAGE_kmod-fs-ntfs=y
        CONFIG_PACKAGE_kmod-fs-vfat=y
        CONFIG_PACKAGE_kmod-usb3=y
        CONFIG_PACKAGE_kmod-usb2=y
        CONFIG_PACKAGE_kmod-tcp-bbr=y

        #================================
        # 编译设置
        #================================
        CONFIG_DEVEL=y
        CONFIG_CCACHE=y

        # 内核配置
        CONFIG_KERNEL_BUILD_USER="OpenWrt"
        CONFIG_KERNEL_BUILD_DOMAIN="openwrt.org"
        
        # 镜像设置
        CONFIG_TARGET_ROOTFS_PARTSIZE=512
        CONFIG_TARGET_ROOTFS_SQUASHFS=y
        CONFIG_GRUB_IMAGES=y
        EOF
        
        make defconfig

    #==============================================================
    # 8. 分步构建工具链
    #==============================================================
    - name: Build toolchain step by step
      run: |
        cd openwrt
        echo "=== Building tools ==="
        make tools/install -j$(nproc) V=s
        echo "=== Building toolchain ==="
        make toolchain/install -j$(nproc) V=s

    #==============================================================
    # 9. 下载所有包
    #==============================================================
    - name: Download all packages
      run: |
        cd openwrt
        for i in {1..3}; do
          echo "Download attempt $i/3"
          if make download -j4; then
            echo "Download successful"
            break
          elif [ $i -eq 3 ]; then
            echo "Final download attempt failed, continuing anyway"
          else
            echo "Download attempt $i failed, retrying in 10 seconds..."
            sleep 10
          fi
        done

    #==============================================================
    # 10. 第一阶段编译（基本系统）
    #==============================================================
    - name: Stage 1 build (basic system)
      run: |
        cd openwrt
        echo "=== Stage 1: Building basic system ==="
        # 先编译基础系统，不包括复杂插件
        make -j2 V=s 2>&1 | tee build-stage1.log || echo "Stage 1 build completed with warnings"

    #==============================================================
    # 11. 添加复杂插件配置
    #==============================================================
    - name: Add complex plugins config
      run: |
        cd openwrt
        # 添加复杂的网络插件
        cat >> .config << "EOF"

        #================================
        # 第二阶段：复杂网络插件
        #================================
        CONFIG_PACKAGE_luci-app-turboacc=y
        CONFIG_PACKAGE_luci-app-adguardhome=y
        CONFIG_PACKAGE_luci-app-smartdns=y
        CONFIG_PACKAGE_luci-app-ddns-go=y
        CONFIG_PACKAGE_luci-app-lucky=y
        EOF
        
        make defconfig

    #==============================================================
    # 12. 第二阶段编译（网络插件）
    #==============================================================
    - name: Stage 2 build (network plugins)
      run: |
        cd openwrt
        echo "=== Stage 2: Building network plugins ==="
        make package/feeds/packages/adguardhome/compile -j1 V=s 2>&1 | tee build-adguardhome.log || echo "AdGuardHome build may have issues"
        make package/feeds/packages/smartdns/compile -j1 V=s 2>&1 | tee build-smartdns.log || echo "SmartDNS build may have issues"

    #==============================================================
    # 13. 添加代理插件配置
    #==============================================================
    - name: Add proxy plugins config
      run: |
        cd openwrt
        # 添加代理相关插件
        cat >> .config << "EOF"

        #================================
        # 第三阶段：代理插件
        #================================
        CONFIG_PACKAGE_luci-app-openclash=y
        CONFIG_PACKAGE_luci-app-passwall=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
        CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Trojan=y
        EOF
        
        make defconfig

    #==============================================================
    # 14. 第三阶段编译（代理插件）
    #==============================================================
    - name: Stage 3 build (proxy plugins)
      run: |
        cd openwrt
        echo "=== Stage 3: Building proxy plugins ==="
        make package/feeds/packages/passwall/compile -j1 V=s 2>&1 | tee build-passwall.log || echo "PassWall build may have issues"
        make package/feeds/packages/openclash/compile -j1 V=s 2>&1 | tee build-openclash.log || echo "OpenClash build may have issues"

    #==============================================================
    # 15. 完整系统编译
    #==============================================================
    - name: Final full build
      run: |
        cd openwrt
        echo "=== Final: Full system build ==="
        # 最终完整编译，使用较低的并行度避免内存问题
        make -j2 V=s 2>&1 | tee build-final.log

    #==============================================================
    # 16. 显示构建结果
    #==============================================================
    - name: Show build results
      if: always()
      run: |
        cd openwrt
        echo "=== Firmware files ==="
        find bin/targets/ -type f \( -name "*.img" -o -name "*.gz" -o -name "*.bin" \) 2>/dev/null | head -20 || echo "No firmware files found"
        echo "=== Disk usage ==="
        df -h .
        echo "=== Memory usage ==="
        free -h
        echo "=== Recent build errors ==="
        find . -name "*.log" -type f -exec grep -l -i "error\|failed" {} \; | head -5

    #==============================================================
    # 17. 上传固件
    #==============================================================
    - name: Upload firmware
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: OpenWrt-x86_64-Custom-Plugins
        path: openwrt/bin/targets/x86/64/
        retention-days: 7

    #==============================================================
    # 18. 上传构建日志
    #==============================================================
    - name: Upload build logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: Build-Logs-Detailed
        path: |
          openwrt/*.log
          openwrt/build_dir/
          openwrt/logs/
        retention-days: 3
